import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { LoggerRoutingModule } from './logger.routing.module';

@NgModule({
  imports: [CommonModule, LoggerRoutingModule],
  declarations: [],
})
export class LoggerModule {}
